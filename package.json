{
  "name": "ts-minify",
  "version": "0.0.1",
  "description": "A tool to aid minification of Typescript code, using Typescript's type information.",
  "repository": "https://github.com/angular/ts-minify",
  "license": "Apache-2.0",
  "scripts": {
    "prepublish": "tsd query typescript --action install && tsd query node --action install && tsd query mocha --action install"
  },
  "dependencies": {
    "typescript": ">=1.5.0-beta",
    "gulp": ">=3.9.0",
    "shrinkwrap": "latest",
    "gulp-debug": ">=2.0.1",
    "gulp-inject": ">=1.3.1",
    "gulp-typescript": ">=2.7.7",
    "gulp-tslint": ">=3.0.1-beta",
    "gulp-sourcemaps": ">=1.5.2",
    "del": ">=1.2.0",
    "es6-map": ">=0.1.1",
    "fs-extra": "^0.18.4"
  },
  "devDependencies": {
    "clang-format": "^1.0.25",
    "gulp-clang-format": "^1.0.21",
    "gulp-mocha": "^2.1.1",
    "chai": "*",
    "mocha": "*",
    "gulp-util": ">=3.0.6",
    "merge2": "0.3.6"
  }
}
